services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: obeeoma-frontend-new
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - REACT_APP_API_URL=/api